spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://postgres/docker
    username: dbuser
    password: dbpassword
    driver-class-name: org.postgresql.Driver
