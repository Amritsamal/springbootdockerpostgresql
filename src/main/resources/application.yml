spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://postgreserver/docker
    username: postgres
    password: dbpassword
    driver-class-name: org.postgresql.Driver
