spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://172.17.0.9:5432/docker
    username: postgres
    password: dbpassword
    driver-class-name: org.postgresql.Driver
