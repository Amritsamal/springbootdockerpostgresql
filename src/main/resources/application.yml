spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://192.168.59.103:5432/docker
    username: dbuser
    password: dbpassword
    driver-class-name: org.postgresql.Driver
